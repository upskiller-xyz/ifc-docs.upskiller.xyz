# Local development only. Deployment runs in GitHub Actions (see DEPLOYMENT.md)
# and takes its credentials from repository secrets, not from this file.

NODE_ENV=development

# Port for `npm start` / `npm run serve`
PORT=3000
